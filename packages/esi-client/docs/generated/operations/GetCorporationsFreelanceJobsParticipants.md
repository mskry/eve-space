<!--
@generated by @evespace/esi-client.
Compatibility date: 2026-08-18.
Specification SHA-256: 7d3f738c3c363b400a5cdf78db4432c19ae75c13de2b7816f21970e8c7ea9c2c.
DO NOT EDIT.
-->

# GetCorporationsFreelanceJobsParticipants

List participants of a freelance job

- Stable ID: `GetCorporationsFreelanceJobsParticipants`
- HTTP: `GET /corporations/{corporation_id}/freelance-jobs/{job_id}/participants`
- Domain method: `client.freelanceJobs.listParticipants(corporationId, jobId, options?)`
- Generic call: `client.callOperation("GetCorporationsFreelanceJobsParticipants", arguments, callOptions?)`
- Domain import: `@evespace/esi-client/domains/freelance-jobs`
- Domain index: [freelanceJobs](../domains/freelance-jobs.md)

Required path identifiers are positional in the domain method. Other request values and an available compatibility-date override are fields in its final options object. Generic arguments use `path`, `query`, `headers`, and `body` groups matching the parameter table.

## Standalone domain-factory snippet

```ts
import { createFreelanceJobsClient } from '@evespace/esi-client/domains/freelance-jobs';
import type { GetCorporationsFreelanceJobsParticipantsResponse } from '@evespace/esi-client/types';

const accessToken = process.env.ESI_ACCESS_TOKEN;
if (!accessToken) throw new Error('Set ESI_ACCESS_TOKEN before making this authorized request.');

const client = createFreelanceJobsClient({ token: accessToken });

const corporationId = 98000001;
const jobId = "00000000-0000-4000-8000-000000000000";

const data: GetCorporationsFreelanceJobsParticipantsResponse = await client.listParticipants(corporationId, jobId);
```

## Aggregate EsiClient snippet

```ts
import { EsiClient } from '@evespace/esi-client';
import type { GetCorporationsFreelanceJobsParticipantsResponse } from '@evespace/esi-client/types';

const accessToken = process.env.ESI_ACCESS_TOKEN;
if (!accessToken) throw new Error('Set ESI_ACCESS_TOKEN before making this authorized request.');

const client = new EsiClient({ token: accessToken });

const corporationId = 98000001;
const jobId = "00000000-0000-4000-8000-000000000000";

const data: GetCorporationsFreelanceJobsParticipantsResponse = await client.freelanceJobs.listParticipants(corporationId, jobId);
```

## Generic-execution snippet

```ts
import { EsiClient } from '@evespace/esi-client';
import type { CallOperationArguments } from '@evespace/esi-client/operations';
import type { GetCorporationsFreelanceJobsParticipantsData, GetCorporationsFreelanceJobsParticipantsResponse } from '@evespace/esi-client/types';

const accessToken = process.env.ESI_ACCESS_TOKEN;
if (!accessToken) throw new Error('Set ESI_ACCESS_TOKEN before making this authorized request.');

const client = new EsiClient({ token: accessToken });

const corporationId = 98000001;
const jobId = "00000000-0000-4000-8000-000000000000";

const arguments_: CallOperationArguments<'GetCorporationsFreelanceJobsParticipants'> = { path: { "corporation_id": corporationId, "job_id": jobId } };

const response = await client.callOperation('GetCorporationsFreelanceJobsParticipants', arguments_);
const data: GetCorporationsFreelanceJobsParticipantsResponse = response.data;
```

## Parameters

| Name | Placement | Required | Schema | Description |
| --- | --- | --- | --- | --- |
| corporation_id | path | yes | reference #/components/schemas/CorporationID | The ID of the corporation |
| job_id | path | yes | reference #/components/schemas/UUID | The ID of the job |
| after | query | no | string | Return records from after this cursor (mutual exclusive with 'before'). '0' to start from the beginning. |
| before | query | no | string | Return records from before this cursor (mutual exclusive with 'after'). '0' to start from the end. |
| limit | query | no | integer (int64) | The amount of records to retrieve per request. |
| If-Modified-Since | header | no | string | The date the resource was last modified. A 304 will be returned if the resource has not been modified since this date. |
| If-None-Match | header | no | string | The ETag of the previous request. A 304 will be returned if this matches the current ETag. |
| X-Tenant | header | no | string | The tenant ID for the request. |

## Result and schemas

- Request type: `@evespace/esi-client/types` export `GetCorporationsFreelanceJobsParticipantsData`
- Request-layer schemas: `headers` uses `@evespace/esi-client/zod` export `zGetCorporationsFreelanceJobsParticipantsHeaders`; `path` uses `@evespace/esi-client/zod` export `zGetCorporationsFreelanceJobsParticipantsPath`; `query` uses `@evespace/esi-client/zod` export `zGetCorporationsFreelanceJobsParticipantsQuery`.
- Response type: `@evespace/esi-client/types` export `GetCorporationsFreelanceJobsParticipantsResponse`
- Domain result: bare validated success data; a no-content response resolves to `undefined`.
- Metadata result: `client.freelanceJobs.withMetadata().listParticipants(...)` returns `EsiResponse<T>`.
- Generic result: `callOperation` returns one serializable `EsiResponse<T>` envelope.

| Status | Body | Schema module | Schema export | Description |
| --- | --- | --- | --- | --- |
| `200` | json | `@evespace/esi-client/zod` | `zGetCorporationsFreelanceJobsParticipantsResponse` | OK |

## Authentication

Required scopes: `esi-corporations.read_freelance_jobs.v1`.

## Pagination and cache

Pagination: none declared. Generic execution still performs exactly one request.

Cache response headers: `cache-control`, `etag`, `last-modified`. Cache extensions: `x-cache-mode=event-based`, `x-server-cache-mode=event-based`. Metadata-enabled and generic results expose normalized cache fields plus all original response headers.

Declared conditional request validators: `if-modified-since`, `if-none-match`. These declarations identify accepted request headers; they do not by themselves assert support for a `304 Not Modified` response.

Route rate limit: group `corp-freelance-job`, maximum 300 tokens per `15m` window.

Bounded request arrays: none declared. Maximum batch size: unspecified.

## Mutation safety

This operation is classified as a read and does not use generic mutation gates.

## Structured errors

| Error | Code | Behavior |
| --- | --- | --- |
| `EsiRequestValidationError` | `ESI_REQUEST_VALIDATION_ERROR` | Invalid request arguments; generic calls always validate before network activity. |
| `EsiResponseValidationError` | `ESI_RESPONSE_VALIDATION_ERROR` | A successful response does not match the generated schema. |
| `EsiHttpError` | `ESI_HTTP_ERROR` | ESI returns a non-success status; includes status, metadata, and a bounded body when available. |
| `EsiResponseParseError` | `ESI_RESPONSE_PARSE_ERROR` | A successful JSON response cannot be parsed. |
| `EsiAuthenticationRequiredError` | `ESI_AUTHENTICATION_REQUIRED` | No credential is available; includes required scopes. |

Error serialization is allowlisted and excludes credentials and authorization headers.

## Standalone examples

- [Authenticated](../examples/authenticated.md)
- [Metadata](../examples/metadata.md)
- [Operation protocol](../examples/operation-protocol.md)
- [Schema validation](../examples/schema-validation.md)
- [Validation error](../examples/validation-error.md)

## Shared concepts

- [Authentication](../concepts/auth.md)
- [Validation](../concepts/validation.md)
- [Metadata and pagination](../concepts/metadata-pagination.md)
- [Structured errors](../concepts/errors.md)
- [Mutation safety](../concepts/mutation-safety.md)
