// @generated by @evespace/esi-client.
// Compatibility date: 2026-08-18.
// Specification SHA-256: d109f3a545525dd98ac0d8237c7838b3bfcb30cb1d971166959b633bac22d599.
// DO NOT EDIT.

import { z } from 'zod';

type IsExact<Left, Right> =
  (<Value>() => Value extends Left ? 1 : 2) extends <Value>() => Value extends Right ? 1 : 2
    ? (<Value>() => Value extends Right ? 1 : 2) extends <Value>() => Value extends Left ? 1 : 2
      ? true
      : false
    : false;

type Assert<Value extends true> = Value;

function canonicalJson(value: unknown): string {
  return (
    JSON.stringify(value, (_key: string, current: unknown): unknown => {
      if (current === null || typeof current !== 'object' || Array.isArray(current)) return current;
      return Object.fromEntries(
        Object.entries(current).sort(([left], [right]) =>
          left < right ? -1 : left > right ? 1 : 0,
        ),
      );
    }) ?? 'undefined'
  );
}

function isJsonArrayUnique(values: readonly unknown[]): boolean {
  const seen = new Set<string>();
  for (const value of values) {
    const key = canonicalJson(value);
    if (seen.has(key)) return false;
    seen.add(key);
  }
  return true;
}

type UniverseStructuresGetSchemaInput = Array<number>;
type UniverseStructuresGetSchemaOutput = Array<number>;

export const UniverseStructuresGetSchema: z.ZodType<UniverseStructuresGetSchemaOutput, UniverseStructuresGetSchemaInput> = z.array(z.int()).refine(isJsonArrayUnique, { message: 'Array items must be unique' });
export type UniverseStructuresGetInput = z.input<typeof UniverseStructuresGetSchema>;
export type UniverseStructuresGet = z.output<typeof UniverseStructuresGetSchema>;

type UniverseStructuresGetInputAssertion = Assert<IsExact<UniverseStructuresGetInput, UniverseStructuresGetSchemaInput>>;
type UniverseStructuresGetOutputAssertion = Assert<IsExact<UniverseStructuresGet, UniverseStructuresGetSchemaOutput>>;
